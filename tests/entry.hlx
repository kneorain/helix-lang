[no-entry]
// TODO: this file needs a better name
/**
 *
 */

import all_tests from *; // imports all the modules from this dir and assigns them to all_tests
use all_tests; // makes all the code in all_tests available to this file
// we could have used `import * from *` to do the same thing but this shows the use of use

control_flow::test_if();
control_flow::test_while();
control_flow::test_for();
control_flow::test_switch();
control_flow::test_loop();
control_flow::test_return();
control_flow::test_break();
control_flow::test_continue();
control_flow::test_label();